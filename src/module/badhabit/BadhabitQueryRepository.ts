import { Injectable } from '@nestjs/common';

@Injectable()
export class BadhabitQueryRepository {
  constructor() {} // private readonly badhabitRepository: EntityRepository<Badhabit>, // @InjectRepository(Badhabit)
}
